name: Deployment
on:  
    push:
    workflow_dispatch:
jobs:
    testing:
        runs-on: [windows-latest]
        steps:
            - name: check out repo
              uses: actions/checkout@v4
            - name: create cache
              id: cache-prems
              uses: actions/cache@v3
              with:
                path: node_modules
                key: dep-cache-${{ hashFiles('./package.json') }}
            - name: git installation
              run: npm install
    deploy:
        needs: testing
        runs-on: windows-latest
        steps:
            - name: check out repotory
              uses: actions/checkout@v4
            - name: restored cache1
              id: cache-prems
              uses: actions/cache@v3
              with:
                path: node_modules  
                key: dep-cache-${{ hashFiles('./package.json') }}

                  
 

